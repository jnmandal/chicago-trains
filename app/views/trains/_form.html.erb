<%= form_for(@train) do |f| %>
  <% if @train.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
      <% @train.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.text_field :line, placeholder: "line" %>
  </div>
  <div class="form-group">
    <%= f.text_field :route, placeholder: "route" %>
  </div>
  <div class="form-group">
    <%= f.text_field :run, placeholder: "run" %>
  </div>
  <div class="form-group">
    <%= f.text_field :operator, placeholder: "operator" %>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: "btn btn-success" %>
  </div>
<% end %>
<p><%= link_to 'Destroy', @train, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></p>